"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function CandidateLoginPage(){const [reference,setReference]=useState(""),[email,setEmail]=useState(""),router=useRouter();return <main className="mx-auto max-w-md px-5 py-20"><p className="text-xs font-bold uppercase tracking-widest text-[#0075de]">Careers</p><h1 className="mt-2 text-4xl font-black">Candidate login</h1><p className="mt-2 text-sm text-slate-600">Use the application reference and email you used to apply.</p><form onSubmit={e=>{e.preventDefault();if(reference.trim()&&email.trim())router.push(`/careers/application/${encodeURIComponent(reference.trim())}?email=${encodeURIComponent(email.trim())}`)}} className="mt-7 space-y-4"><input required value={reference} onChange={e=>setReference(e.target.value)} placeholder="Application reference" className="h-12 w-full rounded-xl border px-3"/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="h-12 w-full rounded-xl border px-3"/><button className="h-12 w-full rounded-xl bg-[#0075de] font-bold text-white">View my application</button></form></main>}
