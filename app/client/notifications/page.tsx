"use client";
import { useQuery } from "@tanstack/react-query";
type Notification = { id: string; message_type: string; subject: string | null; body: string; status: string; created_at: string };
export default function ClientNotificationsPage() { const query = useQuery({ queryKey: ["client", "notifications"], queryFn: async () => { const response = await fetch("/api/client/notifications", { cache: "no-store" }); const body = await response.json(); if (!response.ok) throw new Error(body.error || "Unable to load notifications."); return body as { notifications: Notification[] }; } }); return <main className="mx-auto max-w-4xl p-6 md:p-10"><h1 className="text-3xl font-bold">Notifications</h1><p className="mt-2 text-slate-600">Project, billing, approval, and support updates from GrowXLabs.</p><div className="mt-8 space-y-3">{query.isPending ? <p>Loading notifications…</p> : query.error ? <p role="alert" className="text-red-700">{query.error.message}</p> : query.data.notifications.length ? query.data.notifications.map((item) => <article key={item.id} className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wide text-slate-500">{item.message_type.replaceAll("_", " ")}</p><h2 className="mt-1 font-semibold">{item.subject || "GrowXLabs update"}</h2></div><time className="text-xs text-slate-500">{new Date(item.created_at).toLocaleDateString()}</time></div><div className="mt-3 whitespace-pre-wrap text-sm leading-6 text-slate-700">{item.body}</div></article>) : <div className="rounded-xl border border-dashed border-slate-300 p-8 text-center text-slate-500">No notifications yet.</div>}</div></main>; }
