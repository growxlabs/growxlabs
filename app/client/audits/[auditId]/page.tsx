"use client";
import {useParams} from "next/navigation";import {useQuery} from "@tanstack/react-query";import {AuditDocument} from "@/components/audit/AuditDocument";import type {AuditRecord} from "@/lib/audits/presentation";
export default function ClientAuditPage(){const {auditId}=useParams<{auditId:string}>();const query=useQuery<{audit:AuditRecord}>({queryKey:["client-audit",auditId],queryFn:async()=>{const r=await fetch(`/api/client/audits/${auditId}`,{cache:"no-store"}),b=await r.json();if(!r.ok)throw new Error(b.error||"Audit report is unavailable.");return b}});if(query.isPending)return <main className="mx-auto max-w-4xl p-8">Loading report…</main>;if(query.error)return <main className="mx-auto max-w-4xl p-8 text-red-700">{query.error.message}</main>;return <main className="py-4"><AuditDocument audit={query.data.audit}/></main>}
