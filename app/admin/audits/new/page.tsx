"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function NewAuditPage(){const router=useRouter();const [meetingId,setMeetingId]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();setBusy(true);setError("");const r=await fetch("/api/admin/audits",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({discoveryMeetingId:meetingId})});const b=await r.json();if(!r.ok){setError(b.error||"Unable to create audit.");setBusy(false);return}router.push(`/admin/audits/${b.audit.id}`);}return <main className="mx-auto max-w-2xl p-6 md:p-10"><p className="text-xs font-bold uppercase tracking-widest text-slate-500">Consulting deliverable</p><h1 className="mt-2 text-3xl font-bold">Create Business & Technical Audit</h1><p className="mt-2 text-sm text-slate-600">Only a closed Discovery Meeting can create an audit report.</p><form onSubmit={submit} className="mt-8 space-y-5 rounded-xl border border-slate-200 bg-white p-6"><label className="grid gap-2 text-sm font-semibold">Discovery Meeting ID<input required value={meetingId} onChange={e=>setMeetingId(e.target.value)} placeholder="Paste the closed meeting UUID" className="rounded-lg border border-slate-300 px-3 py-2 font-normal"/></label>{error&&<p role="alert" className="text-sm text-red-700">{error}</p>}<button disabled={busy} className="rounded-lg bg-[#1d4f7a] px-5 py-3 text-sm font-semibold text-white disabled:opacity-50">{busy?"Building report…":"Build audit from discovery"}</button></form></main>}
