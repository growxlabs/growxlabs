import { ProjectWorkspaceDetail } from "@/components/admin/delivery/ProjectWorkspaceDetail";
export default async function ProjectPage({ params }: { params: Promise<{ projectId: string }> }) { const { projectId } = await params; return <main className="mx-auto max-w-7xl space-y-6 p-6 md:p-8"><a className="text-sm font-semibold text-[#164d75]" href="/admin/projects">← Project workspaces</a><div><h1 className="text-3xl font-bold">Delivery Workspace</h1><p className="mt-2 text-slate-600">Milestones and tasks remain traceable to the approved project scope.</p></div><ProjectWorkspaceDetail projectId={projectId} /></main>; }
