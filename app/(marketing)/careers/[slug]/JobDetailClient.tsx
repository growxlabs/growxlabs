"use client";
import Link from "next/link";
import { ArrowLeft, BriefcaseBusiness, MapPin } from "lucide-react";
import { ApplicationForm } from "../CareersContent";
type Job={id:string;title:string;slug:string;summary:string;description:string|Record<string,unknown>;responsibilities:string[];requirements:string[];skills:string[];employmentType:string;location:string;isRemote:boolean;salaryMin?:number;salaryMax?:number;salaryCurrency:string;benefits:string[]};
export default function JobDetailClient({job}:{job:Job|null}){if(!job)return <main className="flex min-h-screen items-center justify-center"><div className="text-center"><h1 className="text-3xl font-black">Role not found</h1><Link href="/careers" className="mt-5 inline-block text-sm font-bold text-[#0075de]">View open roles</Link></div></main>;const description=typeof job.description==="string"?job.description:JSON.stringify(job.description);return <main className="min-h-screen bg-slate-50 px-5 pb-20 pt-28 text-slate-950"><article className="mx-auto max-w-4xl"><Link href="/careers" className="inline-flex items-center gap-2 text-xs font-bold text-slate-500"><ArrowLeft size={14}/>All open roles</Link><header className="mt-8 rounded-3xl border border-slate-200 bg-white p-8 sm:p-12"><div className="text-[10px] font-bold uppercase tracking-[.2em] text-[#0075de]">Open position</div><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-5xl">{job.title}</h1><p className="mt-5 text-lg leading-8 text-slate-600">{job.summary}</p><div className="mt-6 flex flex-wrap gap-5 text-sm font-semibold text-slate-500"><span className="flex items-center gap-2"><MapPin size={15}/>{job.location}{job.isRemote?" · Remote":""}</span><span className="flex items-center gap-2"><BriefcaseBusiness size={15}/>{job.employmentType}</span></div><div className="mt-8"><ApplicationForm job={job}/></div></header><div className="mt-6 grid gap-6"><Section title="About the role"><p>{description}</p></Section><Section title="What you will do" items={job.responsibilities}/><Section title="What we are looking for" items={job.requirements}/>{job.skills?.length>0&&<Section title="Skills"><div className="flex flex-wrap gap-2">{job.skills.map(skill=><span key={skill} className="rounded-full bg-blue-50 px-3 py-1.5 text-xs font-bold text-[#0075de]">{skill}</span>)}</div></Section>}{job.benefits?.length>0&&<Section title="Benefits" items={job.benefits}/>}</div></article></main>}
function Section({title,items,children}:{title:string;items?:string[];children?:React.ReactNode}){return <section className="rounded-2xl border border-slate-200 bg-white p-7"><h2 className="text-xl font-black">{title}</h2><div className="mt-4 text-sm leading-7 text-slate-600">{children}{items&&<ul className="space-y-2">{items.map(item=><li key={item} className="flex gap-3"><span className="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-[#0075de]"/>{item}</li>)}</ul>}</div></section>}
