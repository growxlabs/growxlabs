import {z} from "zod";import{adminSalesResponse,reviewSalesQualification}from"@/lib/employee-os/admin-sales-control";const schema=z.object({action:z.enum(["approve","request_clarification"]),message:z.string().trim().max(2000).optional()});export async function POST(request:Request,{params}:{params:Promise<{leadId:string}>}){try{const parsed=schema.safeParse(await request.json());if(!parsed.success)return Response.json({error:"Invalid qualification review"},{status:422});return Response.json({qualification:await reviewSalesQualification({leadId:(await params).leadId,...parsed.data})})}catch(error){return adminSalesResponse(error)}}
