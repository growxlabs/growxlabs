export async function PATCH() { return Response.json({ error: "LEGACY_AGREEMENT_ENDPOINT: sent and executed agreement versions are immutable; use the active MSA version workflow." }, { status: 410 }); }
