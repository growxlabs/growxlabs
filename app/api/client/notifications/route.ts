import { communicationError, requireCommunicationClient, supabaseAdmin } from "@/lib/communications/service";
export async function GET() { try { const { clientId } = await requireCommunicationClient(); const { data, error } = await supabaseAdmin.from("communication_messages").select("id,channel,message_type,subject,body,status,created_at,communication_recipients(email,display_name)").eq("client_id", clientId).order("created_at", { ascending: false }); if (error) throw new Error(error.message); return Response.json({ notifications: data || [] }); } catch (error) { return communicationError(error); } }
