import { requireActiveEmployeeContext } from "@/lib/employee-os/context"; import { employeeDocuments } from "@/lib/employee-os/workspace-data"; import { EmptyState, EmploymentNav, PageHeader } from "@/components/workspace/WorkspaceUI";
export default async function DocumentsPage(){const e=await requireActiveEmployeeContext();const docs=e.permissions.includes("employee.documents.read_own")?await employeeDocuments(e):[];return <><PageHeader eyebrow="My Employment" title="Documents" description="Employment documents owned by your employee record."/><EmploymentNav/>{docs.length?<ul className="divide-y divide-slate-200 border-y border-slate-200">{docs.map(d=><li key={d.id} className="flex justify-between py-4"><span className="text-sm font-medium">{d.name}</span><time className="text-xs text-slate-500">{new Date(d.created_at).toLocaleDateString()}</time></li>)}</ul>:<EmptyState>No documents are available for your employee record.</EmptyState>}</>}
