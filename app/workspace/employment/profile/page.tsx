import { requireActiveEmployeeContext } from "@/lib/employee-os/context"; import { DefinitionList, EmploymentNav, PageHeader } from "@/components/workspace/WorkspaceUI";
export default async function ProfilePage(){const e=await requireActiveEmployeeContext();return <><PageHeader eyebrow="My Employment" title="Profile" description="Identity and contact information associated with your employee account."/><EmploymentNav/><DefinitionList items={[["Name",e.name],["Email",e.email],["Employee code",e.employeeCode]]}/><p className="mt-5 text-xs leading-5 text-slate-500">Role, reporting line, employment status and permissions can only be changed by People Operations. Self-service contact editing will appear when the underlying verified profile fields are available.</p></>}
