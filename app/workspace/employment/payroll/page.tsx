import {EmploymentNav,EmptyState,PageHeader} from "@/components/workspace/WorkspaceUI";import {requireActiveEmployeeContext} from "@/lib/employee-os/context";import {employeePayslips} from "@/lib/employee-os/workspace-data";
export default async function PayrollPage(){const e=await requireActiveEmployeeContext();let rows:any[]=[],error="";try{rows=await employeePayslips(e)}catch(x){error=x instanceof Error?x.message:"Published payslips could not be loaded."}return <><PageHeader eyebrow="My Employment" title="Payroll" description="Only payslips published to your employee record are visible here."/><EmploymentNav/>{error?<p role="alert" className="text-sm text-red-700">{error}</p>:rows.length?<ul className="divide-y border-y">{rows.map(x=><li key={x.id} className="grid gap-3 py-4 text-sm sm:grid-cols-[1fr_repeat(3,auto)] sm:items-center"><div><p className="font-semibold">Payslip</p><p className="text-xs text-slate-500">Published {new Date(x.generated_at).toLocaleDateString()}</p></div><span>Gross ₹{Number(x.gross_earnings).toLocaleString("en-IN")}</span><span>Deductions ₹{Number(x.total_deductions).toLocaleString("en-IN")}</span><strong>Net ₹{Number(x.net_pay).toLocaleString("en-IN")}</strong>{x.pdf_url&&<a className="text-blue-700 hover:underline sm:col-start-4" href={x.pdf_url} target="_blank" rel="noreferrer">View payslip</a>}</li>)}</ul>:<EmptyState>No payslip has been published to your employee record.</EmptyState>}</>}
