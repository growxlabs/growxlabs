import { requireActiveEmployeeContext } from "@/lib/employee-os/context";import { AccountActions } from "@/components/workspace/AccountActions";import { DefinitionList,PageHeader } from "@/components/workspace/WorkspaceUI";
export default async function AccountPage(){const e=await requireActiveEmployeeContext();return <><PageHeader eyebrow="Account" title="Account and security" description="Your employee authentication account. Permission administration is managed separately."/><DefinitionList items={[["Display name",e.name],["Email",e.email],["Account status","Active"],["Workspace status",e.workspaceStatus]]}/><div className="mt-8"><AccountActions/></div></>}
