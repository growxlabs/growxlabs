import { notFound } from "next/navigation";import { requireActiveEmployeeContext } from "@/lib/employee-os/context";import { getAccessibleOpportunity,SalesAccessError } from "@/lib/employee-os/sales-service";import { SalesHeader } from "@/components/workspace/SalesUI";import { DefinitionList } from "@/components/workspace/WorkspaceUI";import { OpportunityActions } from "@/components/workspace/OpportunityActions";
export default async function OpportunityPage({params}:{params:Promise<{opportunityId:string}>}){try{const o=await getAccessibleOpportunity(await requireActiveEmployeeContext(),(await params).opportunityId);return <><SalesHeader title={o.name} description={`Opportunity · ${o.stage.replaceAll("_"," ")}`}/><DefinitionList items={[["Prospect",(o.leads as any)?.business_name],["Contact",(o.leads as any)?.name],["Problem / need",o.problem_need],["Potential fit",o.potential_fit],["Qualification summary",o.qualification_summary],["Next action",o.next_action],["Stage",o.stage.replaceAll("_"," ")]]}/>{o.handoff&&<section className="mt-10"><h2 className="mb-3 font-semibold">Discovery brief</h2><pre className="whitespace-pre-wrap bg-slate-100 p-4 text-sm leading-6">{JSON.stringify(o.handoff.discovery_brief,null,2)}</pre></section>}<OpportunityActions opportunity={o}/></>}catch(e){if(e instanceof SalesAccessError)notFound();throw e}}
