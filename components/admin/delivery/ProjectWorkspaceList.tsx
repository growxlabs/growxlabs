"use client";
import { FormEvent, useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
type Workspace = { id: string; project_id: string; status: string; health: string; summary: Record<string, unknown>; created_at: string; consulting_projects: { project_number: string } | null };
const fetchWorkspaces = async () => { const response = await fetch("/api/admin/delivery/workspaces", { cache: "no-store" }); const body = await response.json(); if (!response.ok) throw new Error(body.error || "Unable to load project workspaces."); return body as { workspaces: Workspace[] }; };
export function ProjectWorkspaceList() { const qc = useQueryClient(); const [projectId, setProjectId] = useState(""); const query = useQuery({ queryKey: ["admin", "project-workspaces"], queryFn: fetchWorkspaces }); const activate = useMutation({ mutationFn: async () => { const response = await fetch("/api/admin/delivery/workspaces", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ projectId }) }); const body = await response.json(); if (!response.ok) throw new Error(body.error || "Workspace activation failed."); return body; }, onSuccess: () => { setProjectId(""); qc.invalidateQueries({ queryKey: ["admin", "project-workspaces"] }); } }); const submit = (event: FormEvent) => { event.preventDefault(); if (projectId.trim()) activate.mutate(); }; return <div className="space-y-6"><form className="flex flex-col gap-3 rounded-xl border border-slate-200 bg-white p-5 md:flex-row" onSubmit={submit}><label className="grid flex-1 gap-2"><span className="text-sm font-semibold">Activated consulting project ID</span><input className="min-h-11 rounded-md border border-slate-300 px-3" value={projectId} onChange={(event) => setProjectId(event.target.value)} placeholder="Paste the internal project ID" /></label><button className="min-h-11 self-end rounded-md bg-[#164d75] px-5 font-semibold text-white disabled:opacity-50" disabled={activate.isPending}>Activate workspace</button>{activate.error && <p role="alert" className="self-center text-sm text-red-700">{activate.error.message}</p>}</form>{query.isPending ? <p>Loading projects…</p> : query.error ? <p role="alert" className="text-red-700">{query.error.message}</p> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{query.data.workspaces.map((workspace) => <a key={workspace.id} href={`/admin/projects/${workspace.id}`} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-[#164d75]"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">{workspace.consulting_projects?.project_number || "Project workspace"}</p><h2 className="mt-3 text-xl font-semibold capitalize">{workspace.status.replaceAll("_", " ")}</h2><p className="mt-2 text-sm text-slate-600">Health: {workspace.health.replaceAll("_", " ")}</p></a>)}{!query.data.workspaces.length && <div className="rounded-xl border border-dashed border-slate-300 p-8 text-slate-500">No activated project workspaces.</div>}</div>}</div>; }
