"use client";
import { useEffect,useState } from "react";
import { CalendarCheck,ClipboardCheck,Clock,Settings2 } from "lucide-react";
type Item=Record<string,unknown>;
const sources=[["Attendance policies","/api/v1/hrms/attendance/policies",Settings2],["Shifts","/api/v1/hrms/attendance/shifts",Clock],["Holiday calendars","/api/v1/hrms/attendance/holidays",CalendarCheck],["Leave policies","/api/v1/hrms/leave/policies",ClipboardCheck]] as const;
export default function AttendanceLeaveConsole(){const[data,setData]=useState<Record<string,Item[]>>({}),[error,setError]=useState("");useEffect(()=>{Promise.all(sources.map(async([label,url])=>{const response=await fetch(url),body=await response.json();if(!response.ok)throw new Error(body.detail||body.error);return[label,body.items||[]] as const})).then(entries=>setData(Object.fromEntries(entries))).catch(value=>setError(value instanceof Error?value.message:"Unable to load configuration"))},[]);return <main className="mx-auto max-w-7xl p-6 lg:p-10"><header className="border-b border-neutral-200 pb-7"><p className="text-[10px] font-bold uppercase tracking-[.2em] text-[#0075de]">HR operations</p><h1 className="mt-2 text-3xl font-black">Attendance & leave control centre</h1><p className="mt-2 text-sm text-neutral-500">Versioned policies, shifts, holidays, approvals, and ledger-backed balances.</p></header>{error&&<div className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</div>}<section className="mt-7 grid gap-5 md:grid-cols-2">{sources.map(([label,,Icon])=><article key={label} className="rounded-2xl border border-neutral-200 bg-white p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="rounded-xl bg-blue-50 p-2 text-[#0075de]"><Icon size={18}/></span><h2 className="font-extrabold">{label}</h2></div><strong className="text-2xl">{data[label]?.length??"—"}</strong></div><div className="mt-4 space-y-2">{data[label]?.slice(0,4).map((item,index)=><div key={String(item.id||index)} className="flex justify-between rounded-lg bg-neutral-50 p-3 text-xs"><span className="font-semibold">{String(item.name||item.key||"Configured item")}</span><span className="text-neutral-400">{String(item.status||"active")}</span></div>)}</div></article>)}</section></main>}
