"use client";
import { useMemo,useState } from "react";
import { useQuery,useQueryClient } from "@tanstack/react-query";
import { ColumnDef,flexRender,getCoreRowModel,useReactTable } from "@tanstack/react-table";
import { Brain,BriefcaseBusiness,ChevronRight,Columns3,List,Search,Star,UserRound,X } from "lucide-react";
type Candidate={id:string;name:string;email:string;location:string;skills:string[];currentCompany:string;applicationId:string;job:string;stage:string;matchScore?:number;updatedAt:string};
type Stage={id:string;key:string;name:string;position:number;category:string;isTerminal:boolean};type Pipeline={id:string;name:string;isDefault:boolean;stages:Stage[]};
export default function RecruiterWorkspace(){const client=useQueryClient(),[query,setQuery]=useState(""),[view,setView]=useState<"table"|"kanban">("table"),[selected,setSelected]=useState<Candidate|null>(null);
const candidatesQuery=useQuery<{items:Candidate[]}>({queryKey:["recruitment-candidates",query],queryFn:async()=>{const response=await fetch(`/api/v1/hrms/recruitment/candidates?q=${encodeURIComponent(query)}`);const data=await response.json();if(!response.ok)throw new Error(data.detail||data.error);return data}});
const pipelinesQuery=useQuery<{items:Pipeline[]}>({queryKey:["recruitment-pipelines"],queryFn:async()=>{const response=await fetch("/api/v1/hrms/recruitment/pipelines");const data=await response.json();if(!response.ok)throw new Error(data.detail||data.error);return data}});
const candidates=candidatesQuery.data?.items||[],pipeline=pipelinesQuery.data?.items?.find(item=>item.isDefault)||pipelinesQuery.data?.items?.[0],stages=pipeline?.stages||[];
async function move(candidate:Candidate,stageId:string){const response=await fetch(`/api/v1/hrms/recruitment/applications/${candidate.applicationId}/stage`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({stageID:stageId})});if(response.ok)await client.invalidateQueries({queryKey:["recruitment-candidates"]})}
const columns=useMemo<ColumnDef<Candidate>[]>(()=>[{accessorKey:"name",header:"Candidate",cell:({row})=><div><b>{row.original.name}</b><p className="text-[10px] text-[var(--text-muted)]">{row.original.email}</p></div>},{accessorKey:"job",header:"Job"},{accessorKey:"stage",header:"Stage",cell:({getValue})=><span className="rounded-full bg-blue-500/10 px-2 py-1 text-[9px] font-bold text-[#0075de]">{String(getValue())}</span>},{accessorKey:"currentCompany",header:"Company"},{accessorKey:"skills",header:"Skills",cell:({getValue})=><span className="line-clamp-1">{(getValue() as string[]||[]).join(", ")}</span>},{accessorKey:"matchScore",header:"AI match",cell:({getValue})=><span className="inline-flex items-center gap-1 font-bold"><Star size={12} className="fill-amber-400 text-amber-400"/>{String(getValue()??"—")}</span>}],[]);const table=useReactTable({data:candidates,columns,getCoreRowModel:getCoreRowModel()});
return <div className="space-y-6"><header className="flex flex-col gap-4 border-b border-[var(--border-subtle)] pb-6 sm:flex-row sm:items-end sm:justify-between"><div><div className="mb-2 text-[10px] font-bold uppercase tracking-[.18em] text-[#0075de]">Recruitment · Release 02</div><h1 className="text-3xl font-extrabold">Recruiter workspace</h1><p className="mt-2 text-xs text-[var(--text-secondary)]">Review, score and move candidates through your configured hiring pipeline.</p></div><div className="flex rounded-lg border border-[var(--border-subtle)] p-1"><button onClick={()=>setView("table")} className={`flex items-center gap-2 rounded-md px-3 py-2 text-[10px] font-bold ${view==="table"?"bg-[#0075de] text-white":""}`}><List size={13}/>Table</button><button onClick={()=>setView("kanban")} className={`flex items-center gap-2 rounded-md px-3 py-2 text-[10px] font-bold ${view==="kanban"?"bg-[#0075de] text-white":""}`}><Columns3 size={13}/>Kanban</button></div></header><div className="relative max-w-md"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-[var(--text-muted)]" size={14}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search name, email, skill, company…" className="h-10 w-full rounded-lg border border-[var(--border-subtle)] bg-[var(--card)] pl-9 pr-3 text-xs"/></div>{view==="table"?<div className="overflow-x-auto rounded-xl border border-[var(--border-subtle)] bg-[var(--card)]"><table className="w-full min-w-[900px] text-left text-xs"><thead className="bg-[var(--surface-2)] text-[9px] uppercase tracking-wider text-[var(--text-muted)]">{table.getHeaderGroups().map(group=><tr key={group.id}>{group.headers.map(header=><th key={header.id} className="px-4 py-3">{flexRender(header.column.columnDef.header,header.getContext())}</th>)}</tr>)}</thead><tbody>{table.getRowModel().rows.map(row=><tr key={row.id} onClick={()=>setSelected(row.original)} className="cursor-pointer border-t border-[var(--border-subtle)] hover:bg-[var(--surface-2)]">{row.getVisibleCells().map(cell=><td key={cell.id} className="px-4 py-3">{flexRender(cell.column.columnDef.cell,cell.getContext())}</td>)}</tr>)}</tbody></table></div>:<div className="flex gap-4 overflow-x-auto pb-4">{stages.map(stage=><section key={stage.id} className="w-72 shrink-0 rounded-xl border border-[var(--border-subtle)] bg-[var(--surface-2)]"><header className="flex items-center justify-between border-b border-[var(--border-subtle)] p-3"><b className="text-[10px] uppercase tracking-wider">{stage.name}</b><span className="rounded-full bg-[var(--card)] px-2 py-0.5 text-[9px]">{candidates.filter(candidate=>candidate.stage===stage.name).length}</span></header><div className="space-y-2 p-2">{candidates.filter(candidate=>candidate.stage===stage.name).map(candidate=><article key={candidate.applicationId} onClick={()=>setSelected(candidate)} className="cursor-pointer rounded-lg border border-[var(--border-subtle)] bg-[var(--card)] p-3"><b className="text-xs">{candidate.name}</b><p className="mt-1 text-[10px] text-[var(--text-muted)]">{candidate.job}</p><div className="mt-3 flex items-center justify-between"><span className="flex items-center gap-1 text-[9px] font-bold"><Star size={11} className="fill-amber-400 text-amber-400"/>{candidate.matchScore??"—"}</span>{stages[stage.position]&&<button onClick={event=>{event.stopPropagation();void move(candidate,stages[stage.position].id)}} className="text-[#0075de]"><ChevronRight size={14}/></button>}</div></article>)}</div></section>)}</div>}{!candidatesQuery.isLoading&&!candidates.length&&<div className="py-16 text-center text-xs text-[var(--text-muted)]"><UserRound className="mx-auto mb-3" size={30}/>No candidates match this view.</div>}{selected&&<CandidateFlyout candidate={selected} onClose={()=>setSelected(null)}/>}</div>}
function CandidateFlyout({candidate,onClose}:{candidate:Candidate;onClose:()=>void}){const profile=useQuery<Record<string,unknown>>({queryKey:["candidate",candidate.id],queryFn:async()=>{const response=await fetch(`/api/v1/hrms/recruitment/candidates/${candidate.id}`);const data=await response.json();if(!response.ok)throw new Error(data.detail||data.error);return data}});const timeline=useQuery<{items:{id:string;action:string;occurredAt:string}[]}>({queryKey:["candidate-timeline",candidate.id],queryFn:async()=>{const response=await fetch(`/api/v1/hrms/recruitment/candidates/${candidate.id}/timeline`);return response.json()}});const ai=profile.data?.ai as {summary?:string;match_score?:number;highlights?:string[];concerns?:string[]}|undefined;return <div className="fixed inset-0 z-50 bg-black/30" onClick={onClose}><aside onClick={event=>event.stopPropagation()} className="absolute right-0 top-0 h-full w-full max-w-lg overflow-y-auto border-l border-[var(--border-subtle)] bg-[var(--background)] p-6 shadow-2xl"><div className="flex justify-between"><span className="text-[10px] font-bold uppercase tracking-wider text-[#0075de]">Candidate profile</span><button onClick={onClose}><X size={18}/></button></div><div className="mt-6 flex items-center gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-blue-500/10 text-[#0075de]"><UserRound/></div><div><h2 className="text-xl font-extrabold">{candidate.name}</h2><p className="text-xs text-[var(--text-muted)]">{candidate.email} · {candidate.location}</p></div></div><section className="mt-6 rounded-xl border border-[var(--border-subtle)] p-4"><h3 className="flex items-center gap-2 text-xs font-extrabold"><Brain size={14} className="text-violet-500"/>AI candidate summary</h3><p className="mt-3 text-xs leading-6 text-[var(--text-secondary)]">{ai?.summary||"AI analysis is pending."}</p>{candidate.matchScore!=null&&<div className="mt-4 text-2xl font-black text-violet-600">{candidate.matchScore}<span className="text-xs">/100 match</span></div>}</section><section className="mt-6"><h3 className="text-xs font-extrabold">Activity timeline</h3><div className="mt-3 space-y-3">{timeline.data?.items?.map(item=><div key={item.id} className="border-l-2 border-blue-500/20 pl-4"><b className="text-[11px]">{item.action.replaceAll("."," ")}</b><p className="text-[9px] text-[var(--text-muted)]">{new Date(item.occurredAt).toLocaleString()}</p></div>)}</div></section></aside></div>}
