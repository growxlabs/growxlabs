"use client";import { useMutation } from "@tanstack/react-query";
export function AdminDocumentActions({assessmentId}:{assessmentId:string}){const action=useMutation({mutationFn:async(name:string)=>{const payload=name==="request-information"?{message:"Please provide the additional information requested by our consulting team.",questionKeys:[],sectionKeys:[]}:name==="reopen"?{questionKeys:[],sectionKeys:[]}:{};const response=await fetch(`/api/admin/assessments/${assessmentId}/${name}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const body=await response.json();if(!response.ok)throw new Error(body.error||"Action failed.");return body;}});return <>{["start-review","request-information","reopen"].map((name)=><button key={name} type="button" disabled={action.isPending} onClick={()=>action.mutate(name)} className="min-h-11 rounded border border-slate-300 px-3 text-sm font-semibold capitalize disabled:opacity-50">{name.replaceAll("-"," ")}</button>)}{action.error&&<span role="alert" className="text-xs text-red-700">{action.error.message}</span>}</>}
