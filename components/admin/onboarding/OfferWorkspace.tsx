"use client";
import {FormEvent,useEffect,useMemo,useState} from "react";import {FileSignature,Plus,Send,ShieldCheck,X} from "lucide-react";import {useSearchParams} from "next/navigation";
type Offer={id:string;application_id:string;title:string;status:string;start_date:string;salary:number;currency:string;current_version:number;candidateName:string;applicationReference:string};type Option={id:string;name:string;code?:string;department_id?:string};type Application={id:string;reference:string;candidateName:string;candidateAddress:string;title:string;department:string;location:string;employmentType:string};type Employee={id:string;name:string;designation:string;employee_number:string};
const blank={applicationId:"",title:"",departmentId:"",designationId:"",managerEmployeeId:"",employmentType:"Full-time",workLocation:"",joiningDate:"",salaryAmount:"",salaryCurrency:"INR",probationDays:"90",noticePeriodDays:"30",expiresAt:"",candidateAddress:"",backfillReason:"",workingTerms:"",confidentialityIp:"",termination:"",acceptanceInstructions:""};
const inputClass="h-11 w-full rounded-lg border border-[var(--border-subtle)] bg-[var(--surface-1)] px-3 text-xs text-[var(--text-primary)] outline-none focus:border-[#0075de] focus:ring-2 focus:ring-[#0075de]/15";
export default function OfferWorkspace(){const params=useSearchParams(),[items,setItems]=useState<Offer[]>([]),[applications,setApplications]=useState<Application[]>([]),[departments,setDepartments]=useState<Option[]>([]),[designations,setDesignations]=useState<Option[]>([]),[employees,setEmployees]=useState<Employee[]>([]),[open,setOpen]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[reissue,setReissue]=useState(false),[managerSearch,setManagerSearch]=useState(""),[form,setForm]=useState(blank);
 async function load(){const applicationId=params.get("applicationId"),query=applicationId?`?applicationId=${encodeURIComponent(applicationId)}`:"",r=await fetch(`/api/admin/recruitment/offers${query}`,{cache:"no-store"}),b=await r.json();if(!r.ok)return setMessage(b.error||"Offers could not be loaded");setItems(b.items||[]);setApplications(b.applications||[]);setDepartments(b.departments||[]);setDesignations(b.designations||[]);setEmployees(b.employees||[])}
 function chooseApplication(id:string,source=applications){const a=source.find(x=>x.id===id);if(!a)return setForm(v=>({...v,applicationId:id}));const departmentKey=a.department.trim().toLowerCase(),titleKey=a.title.replace(/\s*\([^)]*\)\s*$/,"").trim().toLowerCase(),titleCode=a.title.match(/\(([^)]+)\)\s*$/)?.[1].trim().toLowerCase(),department=departments.find(x=>x.name.toLowerCase()===departmentKey||x.code?.toLowerCase()===departmentKey),designation=designations.find(x=>x.name.toLowerCase()===titleKey||Boolean(titleCode&&x.code?.toLowerCase()===titleCode));setForm(v=>({...v,applicationId:id,title:a.title||v.title,departmentId:department?.id||v.departmentId,designationId:designation?.id||v.designationId,employmentType:["Full-time","Part-time","Contract","Internship"].includes(a.employmentType)?a.employmentType:v.employmentType,workLocation:a.location||v.workLocation,candidateAddress:a.candidateAddress||v.candidateAddress}))}
 useEffect(()=>{void load()},[]);useEffect(()=>{const id=params.get("applicationId");if(id&&applications.some(application=>application.id===id)){chooseApplication(id);setForm(v=>({...v,backfillReason:params.get("backfill")==="1"?"Existing employee was converted before the formal offer workflow was completed.":v.backfillReason}));setOpen(true)}},[applications,departments,designations,params]);
 const application=applications.find(x=>x.id===form.applicationId),filteredDesignations=designations.filter(x=>!form.departmentId||!x.department_id||x.department_id===form.departmentId),managerOptions=useMemo(()=>employees.filter(x=>`${x.name} ${x.designation} ${x.employee_number}`.toLowerCase().includes(managerSearch.toLowerCase())).slice(0,30),[employees,managerSearch]);
 async function create(event:FormEvent){event.preventDefault();setBusy(true);setMessage("");const payload={...form,reissue,salaryAmount:Number(form.salaryAmount),probationDays:Number(form.probationDays),noticePeriodDays:Number(form.noticePeriodDays),expiresAt:new Date(form.expiresAt).toISOString(),managerEmployeeId:form.managerEmployeeId||null,candidateAddress:form.candidateAddress||null,backfillReason:form.backfillReason||null,terms:{workingTerms:form.workingTerms,confidentialityIp:form.confidentialityIp,termination:form.termination,acceptanceInstructions:form.acceptanceInstructions}};const r=await fetch("/api/admin/recruitment/offers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)}),b=await r.json();setBusy(false);if(!r.ok)return setMessage(b.error||"Draft could not be saved");setMessage(b.missingAdminInputs?.length?`Draft saved. Required before issue: ${b.missingAdminInputs.join(", ")}`:"Draft saved for review.");setOpen(false);setReissue(false);setForm(blank);await load()}
 async function action(id:string,action:"approve"|"issue"){setBusy(true);const r=await fetch(`/api/admin/recruitment/offers/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({action})}),b=await r.json();setBusy(false);setMessage(r.ok?(action==="issue"?(b.emailSent?"Offer issued and emailed.":"Offer issued; email delivery failure was recorded."):"Offer approved and ready to issue."):(b.missingAdminInputs?`Required: ${b.missingAdminInputs.join(", ")}`:[b.error,b.detail].filter(Boolean).join(": ")||"Action failed"));await load()}
 async function offlineAcceptance(id:string){const acceptedAt=window.prompt("Accepted date and time",new Date().toISOString()),evidenceReference=acceptedAt&&window.prompt("Evidence/reference (required)"),note=evidenceReference&&window.prompt("Administrative note (required)");if(!acceptedAt||!evidenceReference||!note)return;setBusy(true);const r=await fetch(`/api/admin/recruitment/offers/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({action:"record_offline_acceptance",acceptedAt,evidenceReference,note})}),b=await r.json();setBusy(false);setMessage(r.ok?"Offline acceptance recorded with evidence and audit trail.":b.error||"Acceptance could not be recorded");await load()}
 return <div className="space-y-6"><header className="flex flex-wrap items-end justify-between gap-4 border-b border-[var(--border-subtle)] pb-6"><div><div className="text-[10px] font-bold uppercase tracking-[.18em] text-[#0075de]">People · Recruitment</div><h1 className="mt-2 text-3xl font-extrabold">Offer management</h1><p className="mt-2 text-xs text-[var(--text-secondary)]">Formal offer drafting, approval, issue, and acceptance.</p></div><button onClick={()=>{setReissue(false);setForm(blank);setOpen(true)}} className="flex items-center gap-2 rounded-lg bg-[#0075de] px-4 py-2 text-xs font-bold text-white"><Plus size={14}/>Create offer</button></header>{message&&<p className="rounded-lg border border-blue-200 bg-blue-50 p-3 text-xs text-blue-800">{message}</p>}<div className="overflow-x-auto rounded-xl border border-[var(--border-subtle)]"><table className="w-full min-w-[850px] text-left text-xs"><thead className="bg-[var(--surface-2)]"><tr>{["Candidate","Application","Role","Joining","Compensation","Version","Status","Actions"].map(x=><th className="px-4 py-3" key={x}>{x}</th>)}</tr></thead><tbody>{items.map(item=><tr key={item.id} className="border-t border-[var(--border-subtle)]"><td className="px-4 py-3 font-semibold">{item.candidateName}</td><td className="px-4 py-3">{item.applicationReference}</td><td className="px-4 py-3">{item.title}</td><td className="px-4 py-3">{item.start_date||"—"}</td><td className="px-4 py-3">{item.currency} {Number(item.salary||0).toLocaleString("en-IN")}</td><td className="px-4 py-3">v{item.current_version}</td><td className="px-4 py-3 font-bold uppercase">{item.status==="sent"?"issued":item.status==="rejected"?"declined":item.status}</td><td className="px-4 py-3"><div className="flex flex-wrap gap-2">{item.status==="draft"&&<button disabled={busy} onClick={()=>void action(item.id,"approve")} className="flex items-center gap-1 rounded border px-2 py-1 text-[10px] font-bold"><ShieldCheck size={11}/>Approve</button>}{item.status==="approved"&&<button disabled={busy} onClick={()=>void action(item.id,"issue")} className="flex items-center gap-1 rounded bg-[#0075de] px-2 py-1 text-[10px] font-bold text-white"><Send size={11}/>Issue</button>}{["sent","accepted"].includes(item.status)&&<a className="rounded border px-2 py-1 text-[10px] font-bold" href={`/api/admin/recruitment/offers/${item.id}`}>Document</a>}{item.status==="sent"&&<button disabled={busy} onClick={()=>void offlineAcceptance(item.id)} className="rounded border border-amber-300 px-2 py-1 text-[10px] font-bold text-amber-800">Record offline acceptance</button>}{["sent","accepted","rejected","expired"].includes(item.status)&&<button onClick={()=>{setReissue(true);setForm({...blank,applicationId:item.application_id,title:item.title});setOpen(true)}} className="rounded border px-2 py-1 text-[10px] font-bold">New version</button>}</div></td></tr>)}</tbody></table>{!items.length&&<div className="py-16 text-center text-xs text-[var(--text-muted)]"><FileSignature className="mx-auto mb-3"/>No offers yet.</div>}</div>{open&&<div className="fixed inset-0 z-[400] flex items-center justify-center bg-black/55 p-4" onMouseDown={()=>setOpen(false)}><form onSubmit={create} onMouseDown={e=>e.stopPropagation()} className="max-h-[94vh] w-full max-w-4xl overflow-y-auto rounded-2xl bg-[var(--background)] p-6"><div className="flex items-start justify-between"><div><h2 className="text-xl font-black">{reissue?"Create new offer version":"Create formal offer"}</h2><p className="mt-1 text-xs text-[var(--text-secondary)]">Confirm approved employment terms. Internal database identifiers remain hidden.</p></div><button type="button" onClick={()=>setOpen(false)} aria-label="Close"><X size={18}/></button></div><section className="mt-6 rounded-xl border bg-[var(--surface-2)] p-4"><Field label="Application"><select required value={form.applicationId} onChange={e=>chooseApplication(e.target.value)} className="input"><option value="">Select candidate application</option>{applications.map(a=><option value={a.id} key={a.id}>{a.candidateName} · {a.title} · {a.reference}</option>)}</select></Field>{application&&<dl className="mt-4 grid gap-3 text-xs sm:grid-cols-3"><div><dt className="muted">Candidate</dt><dd className="font-bold">{application.candidateName}</dd></div><div><dt className="muted">Position</dt><dd className="font-bold">{application.title}</dd></div><div><dt className="muted">Application</dt><dd className="font-bold">{application.reference}</dd></div></dl>}</section>{form.backfillReason&&<div className="mt-4 rounded-lg border border-amber-200 bg-amber-50 p-3 text-xs text-amber-900"><strong>Recovery record</strong><p className="mt-1">This offer is being created for an employee whose recruitment conversion was completed before the formal offer workflow.</p></div>}<Section title="Employment"><Field label="Job title"><input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className="input"/></Field><Field label="Department"><select required value={form.departmentId} onChange={e=>setForm({...form,departmentId:e.target.value,designationId:""})} className="input"><option value="">Select department</option>{departments.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></Field><Field label="Designation"><select required value={form.designationId} onChange={e=>setForm({...form,designationId:e.target.value})} className="input"><option value="">Select designation</option>{filteredDesignations.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></Field><Field label="Reporting manager"><input value={managerSearch} onChange={e=>setManagerSearch(e.target.value)} placeholder="Search employee…" className="input"/><select value={form.managerEmployeeId} onChange={e=>setForm({...form,managerEmployeeId:e.target.value})} className="input mt-2"><option value="">No reporting manager</option>{managerOptions.map(x=><option key={x.id} value={x.id}>{x.name} · {x.designation}</option>)}</select></Field><Field label="Employment type"><select value={form.employmentType} onChange={e=>setForm({...form,employmentType:e.target.value})} className="input">{["Full-time","Part-time","Contract","Internship"].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Work location"><input required value={form.workLocation} onChange={e=>setForm({...form,workLocation:e.target.value})} placeholder="Remote, office, hybrid, or location" className="input"/></Field><Field label="Joining date"><input required type="date" value={form.joiningDate} onChange={e=>setForm({...form,joiningDate:e.target.value})} className="input"/></Field><Field label="Candidate address"><textarea value={form.candidateAddress} onChange={e=>setForm({...form,candidateAddress:e.target.value})} className="input min-h-20 py-3"/></Field></Section><Section title="Compensation"><Field label="Annual compensation"><input required type="number" min="0" value={form.salaryAmount} onChange={e=>setForm({...form,salaryAmount:e.target.value})} className="input"/></Field><Field label="Currency"><select value={form.salaryCurrency} onChange={e=>setForm({...form,salaryCurrency:e.target.value})} className="input"><option>INR</option><option>USD</option><option>GBP</option><option>EUR</option></select></Field></Section><Section title="Employment terms"><Field label="Probation period"><div className="flex items-center gap-2"><input required type="number" min="0" value={form.probationDays} onChange={e=>setForm({...form,probationDays:e.target.value})} className="input"/><span className="text-xs">days</span></div></Field><Field label="Notice period"><div className="flex items-center gap-2"><input required type="number" min="0" value={form.noticePeriodDays} onChange={e=>setForm({...form,noticePeriodDays:e.target.value})} className="input"/><span className="text-xs">days</span></div></Field><Field label="Offer valid until"><input required type="datetime-local" value={form.expiresAt} onChange={e=>setForm({...form,expiresAt:e.target.value})} className="input"/></Field></Section><details className="mt-6 rounded-xl border p-4"><summary className="cursor-pointer text-xs font-bold uppercase tracking-wider">Additional approved terms</summary><p className="mt-2 text-xs text-[var(--text-secondary)]">Use only People-approved wording. Legal wording is never generated automatically.</p><div className="mt-4 grid gap-4 sm:grid-cols-2">{[["workingTerms","Working terms"],["confidentialityIp","Confidentiality and IP"],["termination","Termination"],["acceptanceInstructions","Acceptance instructions"]].map(([key,label])=><Field key={key} label={label}><textarea required value={(form as any)[key]} onChange={e=>setForm({...form,[key]:e.target.value})} className="input min-h-24 py-3"/></Field>)}</div></details><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="rounded-lg border px-5 py-3 text-xs font-bold">Cancel</button><button disabled={busy} className="rounded-lg bg-[#0075de] px-5 py-3 text-xs font-bold text-white disabled:opacity-50">{busy?"Saving…":"Save draft"}</button></div></form></div>}</div>}
function Section({title,children}:{title:string;children:React.ReactNode}){return <section className="mt-6"><h3 className="mb-3 text-xs font-bold uppercase tracking-[.16em] text-[#0075de]">{title}</h3><div className="grid gap-4 sm:grid-cols-2">{children}</div></section>}function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block text-[10px] font-bold uppercase tracking-wide">{label}<div className="mt-1 normal-case">{children}</div></label>}
