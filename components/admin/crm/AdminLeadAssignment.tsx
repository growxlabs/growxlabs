"use client";import { useEffect,useState } from "react";export function AdminLeadAssignment({leadId}:{leadId:string}){const[employees,setEmployees]=useState<any[]>([]),[value,setValue]=useState(""),[message,setMessage]=useState("");useEffect(()=>{fetch(`/api/admin/crm/leads/${leadId}/assignment`).then(r=>r.json()).then(b=>setEmployees(b.employees||[]))},[leadId]);async function assign(){const r=await fetch(`/api/admin/crm/leads/${leadId}/assignment`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({employeeId:value})});const b=await r.json();setMessage(r.ok?"Assignment saved and employee notified.":b.error||"Assignment failed")}return <section className="rounded-xl border border-white/10 bg-white/[0.03] p-4"><h2 className="text-xs font-bold uppercase tracking-wider text-white/70">Employee assignment</h2><div className="mt-3 flex gap-2"><select value={value} onChange={e=>setValue(e.target.value)} className="min-w-0 flex-1 rounded-md bg-neutral-900 px-3 py-2 text-xs text-white"><option value="">Select eligible employee</option>{employees.map(e=><option key={e.id} value={e.id}>{e.name}</option>)}</select><button disabled={!value} onClick={()=>void assign()} className="rounded-md bg-blue-600 px-4 py-2 text-xs font-bold text-white disabled:opacity-50">Assign</button></div>{message&&<p className="mt-2 text-xs text-white/60">{message}</p>}</section>}
