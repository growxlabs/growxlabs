import Link from "next/link";import { PageHeader } from "./WorkspaceUI";
export function SalesHeader({title,description}:{title:string;description:string}){return <><PageHeader eyebrow="Sales" title={title} description={description}/><nav aria-label="Sales workspace" className="mb-8 flex gap-1 overflow-x-auto border-b border-slate-200 pb-3">{[["Home","/workspace/sales"],["Leads","/workspace/sales/leads"],["Accounts","/workspace/sales/accounts"],["Contacts","/workspace/sales/contacts"],["Follow-ups","/workspace/sales/follow-ups"],["Opportunities","/workspace/sales/opportunities"],["Meetings","/workspace/sales/meetings"]].map(([l,h])=><Link key={h} href={h} className="shrink-0 px-3 py-2 text-sm text-slate-600 hover:bg-slate-100 hover:text-slate-950">{l}</Link>)}</nav></>}
export function LeadRows({leads}:{leads:any[]}){return <ul className="divide-y divide-slate-200 border-y border-slate-200">{leads.map(lead=><li key={lead.id} className="grid gap-3 py-5 sm:grid-cols-[1fr_auto] sm:items-center"><div><h2 className="text-sm font-semibold text-slate-950">{lead.business_name||"Unnamed prospect"}</h2><p className="mt-1 text-sm text-slate-600">{[lead.name,lead.city,lead.source].filter(Boolean).join(" · ")}</p><p className="mt-2 text-xs capitalize text-slate-500">{lead.status?.replaceAll("_"," ")} · {lead.priority} priority</p></div><Link href={`/workspace/sales/leads/${lead.id}`} className="text-sm font-medium text-blue-700 hover:underline">Open lead</Link></li>)}</ul>}
