"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export function AcceptInvitation({token}:{token:string}){const router=useRouter();const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();if(password!==confirm)return setMessage("Passwords do not match.");setBusy(true);const response=await fetch("/api/client/invitations/accept",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password})});const body=await response.json();setBusy(false);if(!response.ok)return setMessage(body.error||"Unable to accept invitation.");router.push("/client/login");}return <main className="mx-auto max-w-lg rounded border border-slate-200 bg-white p-8 shadow-sm"><h1 className="font-serif text-3xl">Create your Client Portal password</h1><p className="mt-3 text-sm leading-6 text-slate-600">Use at least 12 characters with uppercase, lowercase, a number and a symbol.</p><form onSubmit={submit} className="mt-6 grid gap-4"><label className="grid gap-2 text-sm font-semibold">Password<input className="min-h-11 rounded border border-slate-300 px-3" type="password" autoComplete="new-password" value={password} onChange={(event)=>setPassword(event.target.value)}/></label><label className="grid gap-2 text-sm font-semibold">Confirm password<input className="min-h-11 rounded border border-slate-300 px-3" type="password" autoComplete="new-password" value={confirm} onChange={(event)=>setConfirm(event.target.value)}/></label>{message&&<p role="alert" className="text-sm text-red-700">{message}</p>}<button disabled={busy} className="min-h-11 rounded bg-[#1d4f7a] px-5 font-semibold text-white disabled:opacity-60">{busy?"Securing account…":"Accept invitation"}</button></form></main>}
