import {PLAN_VERSION,type ExecutionPlan,type TrustedPlanningContext} from "./plan.types.ts";
export function normalisePlan(untrusted:ExecutionPlan,context:TrustedPlanningContext):ExecutionPlan{return{...untrusted,version:PLAN_VERSION,requestId:context.requestId,conversationId:context.conversationId,organisationId:context.organisationId,workspaceId:context.workspaceId,userId:context.userId,agentId:context.agentId,capabilityId:context.capabilityId,skillId:context.skillId,status:"draft",createdAt:new Date().toISOString(),steps:untrusted.steps.map((step,index)=>({...step,index}))}}
